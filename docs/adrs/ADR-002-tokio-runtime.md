# ADR-002：Tokio-only 异步运行时

**状态**：Accepted　**日期**：2026-07-21

## 背景

数据、Web、MQ 和 task-local 上下文需要一致的取消与调度语义。

## 决策

Rust 1.97.1、Edition 2024，Tokio 是唯一正式运行时；跨边界使用 `Future`/`Stream`。

## 备选方案

async-std 和运行时无关抽象均被拒绝：前者生态覆盖不足，后者会把上下文和驱动差异扩散到所有接口。

## 影响

适配器实现更一致，但使用其他运行时的应用需要在边界桥接。
