# ADR-010：双许可证与独立 SemVer

**状态**：Accepted　**日期**：2026-07-21

## 背景

核心与 RBatis 扩展分属两个组织，发布节奏和上游兼容窗口不同。

## 决策

全部自有 crate 使用 `MIT OR Apache-2.0`，各仓库独立 SemVer，由 ddd4r 维护统一兼容矩阵。

## 备选方案

单版本仓库族会导致无关组件联动发布；单许可证降低部分用户适配性。

## 影响

发布必须执行 semver、许可证、SBOM、provenance 和兼容矩阵门禁。
